script(type="text/javascript").
    var lang = navigator.language;
    var regex = /^fr/;
    if (regex.test(lang))
    { 
        window.location= "//www."+window.location.hostname+":3000/fr/";
    } else {
    	window.location= "//www."+window.location.hostname+":3000/en/";
    }
